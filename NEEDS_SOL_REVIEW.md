# NEEDS_SOL_REVIEW

以下事项涉及仍未实现的产品能力、数据库迁移、发布策略或大规模架构变化，不应在没有部署样本和明确契约时继续猜测式修改。

本轮最终修复已关闭两个原待评审阻断项：

- P1-02 采用安全失效：BungeeCord 模式不再注册入站 `PlayerList` 通道，未认证代理数据不再能驱动删除；`follow-quiting` 在该模式明确禁用。
- P1-03 采用持久化“至少一次”命令日志：每条 pre-spawn 必须有同序号的幂等补偿，外部命令前先写进度，失败时逆序补偿，正常退出/禁用/崩溃重启都会恢复 finalizer；配对不完整或恢复失败时拒绝不安全启动。

## 1. 跨区 invsee 的可写一致性模型（P1-01）

当前跨区镜像已经改为只读，关闭界面也不会把快照写回假人。这消除了过期整包快照恢复物品、覆盖实时物品的直接风险。

若产品必须保留跨区编辑，需要确定：假人背包的唯一写入区域、revision 的生成与持久化、逐槽基线/CAS 失败时的用户体验、拖拽/鼠标快捷操作的原子语义，以及假人同时执行动作时的冲突策略。没有这些契约，不应重新开放整包同步。

## 2. 数据库 schema version 与坏数据迁移（P2-06）

本次仅隔离坏 UUID/枚举行并记录日志，避免单行坏数据中断整批读取；没有自动删除或重写用户数据。现有代码使用 `CREATE TABLE IF NOT EXISTS`，但没有可确认的版本表、已部署 SQLite/MySQL 数据样本、迁移锁和回滚流程。

正式迁移前需要确认数据库方言、备份位置、事务 API、旧版本兼容窗口、坏行修复规则和失败恢复。否则可能把“读路径不崩”变成数据丢失或跨版本不可启动。

## 3. 26.x/legacy NMS 与依赖发布策略（P2-02/P2-09）

26.x provider 已收紧为明确测试目标 `26.1.2` 并增加成员级 capability probe；本次只做了匹配 server JAR 的静态签名核对，没有启动 Paper/Folia smoke test。legacy 全版本发行还依赖各版本 BuildTools/remapped NMS 制品，现代模块能构建不能证明旧版本都可发布。

需要项目级确定支持版本矩阵、NMS 产物缓存/校验、快照依赖固定方式、SBOM/漏洞门禁，以及 CI 是否必须构建每个宣称支持的版本。未经该策略批准，不应通过盲目升级依赖或放宽版本探测“修复”CI。

## 4. NMS adapter 合并（P2-10）

十套版本模块的重复是明确技术债，但合并会同时改变反射/映射、重映射、ServiceLoader 和每个版本的网络行为。需要先确定最低支持版本、公共状态机边界、版本差异接口、分阶段回滚点和每个版本的实服验收，再拆分重构。

## 5. 最终部署验收

本次可以证明源码编译、单元测试和现代 JAR 结构；不能在当前仓库夹具中证明真实 Paper/Folia 的跨区竞争、停服时序、断电后日志恢复、非 OP 权限矩阵、客户端背包界面和所有 legacy remapped 版本。BungeeCord 安全失效可由“无入站注册、无删除调用链”做静态验证，但仍应在代理环境确认运维提示。发布前需隔离测试服务器、代理和测试账号完成这些验收，不应把构建 PASS 等同于运行时 PASS。
